# 03-ready

Tasks here are implementation-ready: concrete file paths/modules to touch, schema/endpoint/interface definitions, build sequencing, a cuttable/non-cuttable scope split where relevant, and a verification plan. An implementer (human or agent) should be able to build this with zero further clarification.

Tasks arrive here from `docs/task-pipeline/02-specs/` via the `advance-task` procedure. They leave this folder only once actually implemented — move the file into `docs/task-pipeline/04-shipped/` and append an `## Outcome` section once the work is built and verified. See `/AGENTS.md` at the repo root for the full pipeline description.
